services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: equalix
      POSTGRES_USER: equalix
      POSTGRES_PASSWORD: equalix
    ports:
      - "5432:5432"
    volumes:
      - equalix-pgdata:/var/lib/postgresql/data

volumes:
  equalix-pgdata:
