# Equalix documentation

Developer documentation for Equalix.
Install mdBook and run:

```bash
cd docs
mdbook serve --open
```

Runnable samples are in [`samples/`](samples/). Source chapters are in [`src/`](src/).
